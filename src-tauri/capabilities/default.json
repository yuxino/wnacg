{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for app windows",
  "windows": ["main", "album-*"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
